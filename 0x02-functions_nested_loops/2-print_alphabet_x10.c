#include<stdio.h>
#include "main.h"
/**
 * main - Entry point
 * Return: Always 0
 */
int main(void)
{
	int 10x_alphabet = 10;

}

void print_alphabet_x10(void);
{
	char alphabets;

	for (10x_alphabet = 0; 10x_alphabet < 10; 10x_alphabet = 10x_alphabet + 1)
	{
		for (alphabets = 'a'; alphabets <= 'z'; alphabets = alphabets + 1)
		{
			_putchar('aphabets');
			_putchar('\n');
		}

	}
		_putchar("%d", 10x_alphabet);
		_putchar('\n');

	return (0);
}
