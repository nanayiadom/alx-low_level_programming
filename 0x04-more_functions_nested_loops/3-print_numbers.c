#include "main.h"
/**
 * print_numbers - prints all numbers
 *
 * return:always 0
 */

void print_numbers(void);
{
	char a;

	for (a = '0'; a < '10'; a++)
	{
		_putchar(c);
	}
	_putchar(c);
	return (0);
}
