#include "main.h"
/**
 * print_numbers - prints all numbers
 *
 * return:always 0
 */

void print_numbers(void);
{
	int a;

	for (a = 0; a < 10; a++)
	{
		_putchar(c);
		_putchar('\n');
	}

	return (0);
}
